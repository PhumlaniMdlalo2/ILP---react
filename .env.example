# Volunteer form endpoint (Formspree).
# Your live form ID is the default in the source; only change this if you
# create a new Formspree form. Set it in Render -> Environment if overridden.
VITE_FORMSPREE_ID=meaqaynj
